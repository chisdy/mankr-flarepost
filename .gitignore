node_modules
dist
.wrangler
wrangler.deploy.toml
.dev.vars
.DS_Store
*.local
.env
.env.*
.worktrees
.superpowers

# CodeGraph: local index plus the Cursor rule that points at its MCP tools,
# which only exist on machines where the server is installed.
.codegraph/
.cursor/rules/codegraph.mdc
